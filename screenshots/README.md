# Screenshots

Drop the submission screenshots here:

- `01-compile-output.png`  — output of `npm run compile` (circuits listed)
- `02-contract-deployed.png` — output of `npm run verify -- --network preview`
